# Copia este archivo como .env y rellena los valores reales.
# NUNCA subas .env al repositorio (ya debe estar en .gitignore).

# ── Variables internas (ya tienen defaults en docker-compose) ─────────────────
# Solo sobreescribe si cambias algo del stack base.
# JWT_SECRET=prccd_secret_2026
# POSTGRES_PASSWORD=prccd_pass
